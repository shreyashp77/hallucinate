export const textParagraphs: string[] = [
  `Large language models process text using a token-based system where words and subwords are mapped to numerical IDs. This tokenization allows the model to handle any input by breaking it down into a known vocabulary of tokens. The speed at which tokens are generated, measured in tokens per second, is a key performance metric for LLM inference. Higher TPS means faster responses, which is crucial for real-time applications like chatbots, code assistants, and translation services.`,
  `The concept of tokens per second is fundamental to understanding LLM performance. When you interact with a model like GPT-4 or Claude, each word is typically one or more tokens. For example, "hello" might be one token, while "unbelievable" splits into "un", "bel", and "ievable". This subword tokenization handles rare words efficiently. Monitoring TPS helps developers optimize deployments and set response time expectations.`,
  `In the early days of computing, time-sharing allowed multiple users to share a single mainframe. Each user got a small slice of processing time, creating the illusion of dedicated access. This 1960s innovation laid the groundwork for modern operating systems. Today's cloud computing takes this further, with virtual machines sharing physical hardware across thousands of users simultaneously.`,
  `The difference between classical programming and machine learning is fundamental. In classical programming, a developer writes explicit rules transforming input to output. In machine learning, the developer provides examples and the system learns rules automatically. This paradigm shift has enabled breakthroughs in image recognition, natural language processing, and game playing where explicit rules are hard to define.`,
  `Neural networks are composed of layers of interconnected nodes, each performing simple math. Their power emerges from depth and scale. Early layers detect edges in an image, middle layers recognize shapes like eyes or wheels, and later layers identify entire objects like faces. This hierarchical representation learning is why deep learning has succeeded across diverse domains.`,
  `When deploying ML models to production, latency and throughput are critical metrics. Latency measures time to first token, while throughput measures tokens generated per second. These often involve trade-offs. Batching improves throughput but increases latency. Streaming reduces perceived latency by showing tokens as they're generated. Understanding these trade-offs is essential for building responsive AI applications.`,
  `Python was created by Guido van Rossum and first released in 1991. Its design emphasizes code readability through significant whitespace and clean syntax. The "batteries included" standard library provides modules for everything from web servers to math. Over three decades, Python has grown from a niche scripting language to one of the most widely used languages, especially in data science and machine learning.`,

  `The Transformer architecture, introduced in the paper "Attention is All You Need" by Vaswani et al. in 2017, revolutionized natural language processing by replacing recurrent neural networks with a purely attention-based mechanism. At its core, the Transformer consists of an encoder and a decoder, each composed of multiple identical layers stacked on top of each other. Each layer contains two main subcomponents: a multi-head self-attention mechanism and a position-wise feed-forward neural network. Around each of these subcomponents, residual connections are employed, followed by layer normalization. The self-attention mechanism allows each position in the input sequence to attend to all positions in the previous layer, computing attention scores that determine how much focus each word should place on every other word. This is done using three learned weight matrices that project the input into query, key, and value representations. The attention output is computed as a weighted sum of the values, where the weights are derived from the dot product similarity between queries and keys, scaled by the square root of the dimension to prevent vanishing gradients. Multi-head attention runs this process multiple times in parallel with different learned projections, allowing the model to attend to information from different representation subspaces at different positions. The outputs from all heads are concatenated and linearly projected to produce the final result. Positional encodings are added to the input embeddings to give the model information about the relative or absolute position of tokens in the sequence, since the attention mechanism itself is permutation-invariant. The feed-forward network consists of two linear transformations with a ReLU activation in between, applied identically to each position separately and independently. The decoder is similar to the encoder but includes an additional cross-attention layer that attends to the encoder output, and its self-attention layer is masked to prevent positions from attending to subsequent positions, preserving the auto-regressive property during generation. Layer normalization is applied before each subcomponent rather than after in the original paper, a variation known as pre-norm that has become standard in modern implementations because it leads to more stable training. The Transformer's ability to process all tokens in parallel during training made it significantly more efficient than recurrent models, enabling training on much larger datasets and leading to the development of pretrained language models like BERT, GPT, and their successors. The architecture has since been adapted for domains beyond NLP, including computer vision with Vision Transformer, speech processing, and even protein folding with AlphaFold. The key innovation was demonstrating that simple attention mechanisms, when scaled up with sufficient data and compute, could outperform carefully designed recurrent architectures. Subsequent research has shown that the depth and width of Transformer models can be scaled to hundreds of billions of parameters, with techniques like mixture of experts, sparse attention patterns, and architectural improvements like rotary positional embeddings and SwiGLU activations further enhancing their capabilities. The Transformer remains the foundation of virtually all modern large language models and continues to drive progress in artificial intelligence.`,

  `The process of training large language models is computationally intensive and involves several distinct phases, each requiring careful optimization and significant hardware resources. The first phase is data collection and preprocessing, where vast amounts of text are gathered from diverse sources including web crawls, books, academic papers, code repositories, and social media platforms. This raw data undergoes extensive filtering to remove low-quality content, duplicates, and personally identifiable information, followed by tokenization using algorithms like Byte-Pair Encoding or WordPiece that break text into subword units. The second phase is pretraining, where the model learns to predict the next token in a sequence given all previous tokens, an objective known as autoregressive language modeling. During pretraining, the model is trained on millions to trillions of tokens over thousands to millions of steps, using optimization algorithms like AdamW with a cosine learning rate schedule that gradually warms up then decays. The training is distributed across thousands of GPUs or TPUs arranged in clusters with high-bandwidth interconnects, using techniques like data parallelism where each device processes a different batch of data, tensor parallelism where individual operations are split across devices, and pipeline parallelism where different layers are placed on different devices. Activation checkpointing trades compute for memory by not storing intermediate activations during the forward pass and recomputing them during backpropagation, while mixed precision training uses FP16 or BF16 arithmetic for most operations with FP32 master weights to balance speed and numerical stability. Gradient accumulation allows effective batch sizes larger than what fits in GPU memory by accumulating gradients over multiple forward passes before performing an optimizer step. The learning rate is typically warmed up from a small value to a maximum over the first few thousand steps, then decayed following a cosine schedule that gradually reduces the rate. Throughout pretraining, the model's loss on a held-out validation set is monitored to detect overfitting or training instabilities, and checkpoints are saved at regular intervals to guard against hardware failures. After pretraining, the model undergoes instruction fine-tuning where it is trained on datasets of human demonstrations showing desired input-output behavior, using supervised learning to align the model with user intent. This is often followed by reinforcement learning from human feedback, where a reward model trained on human preference judgments is used to further optimize the model's outputs through proximal policy optimization or similar algorithms. Throughout all phases, techniques like gradient clipping prevent exploding gradients, weight decay provides regularization, and the model's activations are monitored for signs of numerical instability. The entire training process for a state-of-the-art model can take weeks to months and cost tens of millions of dollars in compute resources, making efficiency optimizations critical for practical feasibility. Research into more efficient training methods continues, with innovations like mixture of experts, sparse attention mechanisms, and improved model architectures constantly pushing the boundaries of what is achievable with given computational budgets.`,

  `Graphics Processing Units have become the dominant hardware platform for deep learning due to their massively parallel architecture well-suited for the matrix operations that form the computational foundation of neural networks. Unlike CPUs which contain a small number of powerful cores optimized for sequential processing, GPUs contain thousands of smaller cores designed to execute the same operation on multiple data elements simultaneously. This architecture is particularly well-suited for the tensor operations at the heart of deep learning, including matrix multiplication, convolution, and element-wise operations. Modern GPUs like NVIDIA's H100 and AMD's MI300X contain specialized tensor cores that accelerate matrix multiply-accumulate operations, delivering teraflops of throughput for mixed precision computations. The memory hierarchy of GPUs includes high-bandwidth global memory, shared memory within thread blocks, and register files, each with different capacities and access speeds that must be carefully managed to achieve optimal performance. Kernel fusion is a common optimization technique where multiple successive operations are combined into a single kernel launch, reducing the overhead of launching separate kernels and improving data locality. FlashAttention is a recent algorithmic innovation that computes exact attention outputs without materializing the full attention matrix in global memory, instead using tiling and recomputation to achieve significant speedups and memory savings for long sequence lengths. The choice of data type significantly impacts training speed and memory usage, with FP32 providing the highest precision but requiring the most memory and bandwidth, while FP16 and BF16 offer reduced precision with substantial performance improvements and minimal impact on model quality when used with techniques like loss scaling. Quantization pushes this further by representing weights and activations with INT8 or even INT4 precision during inference, enabling significant throughput improvements at the cost of some accuracy degradation. Tensor parallelism distributes individual layer operations across multiple GPUs by partitioning weight matrices along different dimensions, allowing models that don't fit in a single GPU's memory to be trained and served. Pipeline parallelism places different layers on different devices, with micro-batching used to keep all devices busy by having each device process a different micro-batch simultaneously. Data parallelism replicates the entire model on each device and synchronizes gradients across devices at each step using all-reduce communication. The optimal combination of these parallelism strategies depends on the model architecture, cluster topology, and communication bandwidth, with automated search algorithms like those used in deepspeed and megatron helping to find efficient configurations. Memory optimizations like activation checkpointing, memory-efficient optimizers, and CPU offloading allow training larger models on limited hardware by trading computation for memory. The continuous advancement of GPU hardware, with each generation offering more cores, higher memory bandwidth, and specialized tensor operations, has been a primary driver of progress in deep learning, enabling the training of increasingly large models that would have been infeasible just a few years ago.`,

  `Prompt engineering is the practice of designing and refining input prompts to obtain desired outputs from large language models. It has emerged as a critical skill for effectively leveraging LLMs, as the quality and structure of prompts directly influence the relevance, accuracy, and safety of model responses. Basic techniques include providing clear instructions with sufficient context, using delimiters to separate prompt sections, and specifying the desired output format to constrain generation. Few-shot prompting includes example input-output pairs to demonstrate desired behavior without describing it in words. Chain-of-thought prompting instructs the model to reason step by step, significantly improving performance on complex reasoning tasks by making internal reasoning explicit. Tree-of-thought generalizes this by exploring multiple reasoning paths simultaneously, allowing backtracking from dead ends and combining insights from different branches. Self-consistency sampling generates multiple independent outputs and selects the most consistent answer by majority voting, improving reliability by marginalizing over generation stochasticity. ReAct prompting interleaves reasoning with actions that interact with external tools like search engines and calculators, enabling the model to gather information beyond its training data and verify reasoning against external sources. Prompt chaining decomposes complex tasks into sequences of simpler subtasks, each handled by a separate prompt where outputs become inputs to subsequent steps. Automated prompt optimization uses gradient-based methods or discrete search to find optimal prompts for specific tasks, treating the prompt as optimizable parameters. Techniques like prompt tuning learn soft prompts as continuous embeddings rather than discrete text, offering more expressiveness than natural language prompts. The field continues evolving rapidly as techniques are combined in creative ways, with the goal of making LLMs more controllable, reliable, and useful across applications.`,

  `Model evaluation is a crucial aspect of developing and deploying large language models, encompassing metrics, benchmarks, and methodologies designed to assess different aspects of performance. Intrinsic evaluation measures language understanding through perplexity, which quantifies how well the model predicts text, with lower values indicating better performance. However, perplexity alone is insufficient for assessing real-world utility as it does not capture task-specific capabilities or alignment with human preferences. Extrinsic evaluation addresses this through task-specific benchmarks testing particular capabilities: question answering on SQuAD and Natural Questions, reading comprehension on RACE and BoolQ, mathematical reasoning on GSM8K and MATH, code generation on HumanEval and MBPP, and general knowledge on MMLU covering 57 subjects across STEM, humanities, and social sciences. Each benchmark has its own protocol, with some using exact match, others using F1, and many using pass at k for code where solutions are correct if any of k samples passes test cases. Human evaluation remains the gold standard for assessing output quality, particularly for open-ended generation where automated metrics fall short. Human raters evaluate outputs along dimensions like helpfulness, harmlessness, and honesty using Likert scales or pairwise comparisons. However, human evaluation is expensive and suffers from inter-annotator disagreement, motivating LLM-based evaluators where a strong model judges another model's outputs, showing reasonable correlation with human judgments at a fraction of the cost. Automated metrics like BLEU and ROUGE are still used but have well-known limitations, including poor correlation with human judgment for creative tasks and sensitivity to lexical overlap rather than semantic equivalence. Newer metrics like BERTScore and COMET use contextual embeddings from pretrained models to compute semantic similarity, achieving better correlation with human judgment. Evaluation also encompasses safety and bias assessment using benchmarks like TruthfulQA for factuality, BBQ for social bias, and RealToxicityPrompts for toxicity, as well as red-teaming where testers actively probe for harmful behaviors. The choice of methodology should be guided by the specific use case, as no single metric captures all aspects of model quality, and benchmark scores do not always translate to real-world performance due to distribution shift between evaluation datasets and actual usage patterns.`,
];
